# F3 Physical-status / derivation-gap matrix

## What F2 permitted

F2 permits the candidate to remain as a **symbolic contract scaffold**:

- finite candidate `E_A_candidate(z)` on the synthetic nonnegative z-grid,
- positive candidate `E_A_candidate(z)`,
- `E_A_candidate(0)=1` normalization under the tested symbolic contract,
- finite positive `1/E_A_candidate(z)`,
- finite nonnegative distance-like integral guard,
- negative-z rejection guard,
- `h0_target_label` leakage guard.

## What F2 did not establish

F2 did not establish any of the following:

- a derived physical `E(z)`,
- a derived physical `H(z)=H0E(z)`,
- an observable luminosity/angular-diameter/comoving distance relation,
- a supernova, BAO, or CMB likelihood,
- a matched ΛCDM reference comparison,
- posterior/evidence-level cosmological support.

## Derivation gaps that remain open

| Gap | Current status | Required before D2 |
|---|---|---|
| Background baseline `B(z; theta_bg)` | Scaffold only | Must be physically specified and normalized |
| Residual/proxy term `R_A(z; theta_proxy)` | Internal candidate proxy | Must be derived or explicitly declared phenomenological |
| Coupling `epsilon` | Sandbox deformation parameter | Must have bounded prior/interpretation and leakage guard |
| Normalization operator `Normalize_0` | Contract operation | Must be algebraically explicit and stable |
| Distance-like integral | Mathematical guard only | Must be tied to observable distance conventions |
| H0 target label | Guarded label | Must not influence model output or tuning |
| Null control | Present as separation guard | Must remain negative-control only |

## Interpretation

The branch is not empty. It has a recoverable scaffold. However, the scaffold is not yet a physical cosmological model. The correct status is **bounded derivation-gap hold**, not victory, not total defeat, and not D2 readiness.
